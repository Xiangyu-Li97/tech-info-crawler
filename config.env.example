# 优质科技信息爬取系统 - 配置文件示例
# 
# 使用方法:
#   1. 复制此文件为 config.env
#   2. 填写下面的配置项
#   3. 不要将 config.env 提交到Git仓库

# Discord Webhook URL (必需)
# 从Discord频道设置中获取
DISCORD_WEBHOOK_URL="https://discord.com/api/webhooks/YOUR_WEBHOOK_ID/YOUR_WEBHOOK_TOKEN"

# OpenAI API Key (可选)
# 用于AI翻译标题和生成中文摘要
# 如果不配置,将跳过AI处理,使用原始英文内容
OPENAI_API_KEY="sk-your-openai-api-key-here"

# 推送文章数量 (可选,默认10)
# 每天推送到Discord的文章数量
TOP_N_ARTICLES=10

# 邮箱地址 (可选)
# 如果配置,将同时发送邮件报告
# 注意: 邮件功能需要Gmail MCP工具支持,在服务器上可能无法使用
# RECIPIENT_EMAIL="your-email@example.com"
